# --------------------------------------------------------------------------
# Postgres-image init env. Copy to `database.env` (gitignored) and set
# real values. POSTGRES_PASSWORD must match DATABASE_PASS in .env.
# --------------------------------------------------------------------------
POSTGRES_DB=apis
POSTGRES_USER=apis
POSTGRES_PASSWORD=secret
